name: login_indilingo
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0   # â† agregar esta lÃ­nea
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # Activa los iconos de Material (esto quitarÃ¡ los cuadros con X en las flechas)
  uses-material-design: true

  # DeclaraciÃ³n de la carpeta de imÃ¡genes
  assets:
    - assets/images/

